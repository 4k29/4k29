{
  "name": "orchard-feed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  }
}
